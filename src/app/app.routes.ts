import { Routes } from '@angular/router';
import { HeadshotGenerator } from './headshot-generator/headshot-generator';

export const routes: Routes = [
  { path: '', component: HeadshotGenerator }
];
